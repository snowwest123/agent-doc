# [OPEN] SQL Agent 完整过程流式调试

## 会话
- session_id: sql-agent-stream
- 症状：用户反馈数据库问答仍然直接返回，未看到完整 Agent 过程流式事件。
- 目标：确认前端请求、SSE 传输、后端事件生成和前端事件解析的实际运行路径。

## 可证伪假设
1. 前端仍调用 `/ask_sql`，没有调用 `/ask_sql_stream`。
2. 后端返回了 SSE，但代理或开发服务器缓冲了响应。
3. `astream_events` 在首个事件之后发生阻塞，导致首事件不可见。
4. 前端 SSE 解析没有及时拆分事件块。
5. 数据库模式分支未进入 `askSqlStream`。

## 运行证据
- 待收集。

## 状态
- 当前阶段：仪表化前
